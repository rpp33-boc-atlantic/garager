name: ci

on:
  push: 
    branches:
      - main
      - '*'

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      -
        name: Run Tests
        run: npm run ci:test
